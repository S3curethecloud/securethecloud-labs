name: Phase 255 Enterprise Agent Developer Integration

on:
  push:
    branches:
      - agent/enterprise-agent-developer-l2-overview
    paths:
      - .github/workflows/phase255-integrate.yml

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Phase 255 branch
        uses: actions/checkout@v4
        with:
          ref